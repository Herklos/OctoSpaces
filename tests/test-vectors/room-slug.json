{
  "description": "Cross-language vector for slugify (ASCII inputs only). Pipeline: lowercase → replace non-[a-z0-9] runs with '-' → strip leading/trailing '-' → truncate 40 → fallback 'item'. Non-ASCII inputs are excluded — TS and Python differ on how accented characters are handled (TS strips them, Python normalises first).",
  "vectors": [
    {
      "input": "Hello World",
      "expected": "hello-world"
    },
    {
      "input": "My-Project 2024",
      "expected": "my-project-2024"
    },
    {
      "input": "ALL CAPS",
      "expected": "all-caps"
    },
    {
      "input": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
      "expected": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
    },
    {
      "input": "",
      "expected": "item"
    },
    {
      "input": "!!! @@@",
      "expected": "item"
    },
    {
      "input": "simple",
      "expected": "simple"
    },
    {
      "input": "abc-123-xyz",
      "expected": "abc-123-xyz"
    }
  ]
}
